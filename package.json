{
  "name": "RNGToken",
  "version": "1.0.0",
  "main": "truffle-config.js",
  "repository": "http://",
  "author": "DengJie",
  "license": "MIT",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.6.0",
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "deploy-local": "del /s build && truffle compile && truffle migrate",
    "deploy-rinkeby": "del /s build && truffle migrate --network rinkeby",
    "test": "truffle test"
  }
}
